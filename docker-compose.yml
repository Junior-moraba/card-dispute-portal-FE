services:
  card-dispute-portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:80"
      - "5173:80"
    container_name: card-dispute-portal
    restart: unless-stopped
    environment:
      - NODE_ENV=production
